# Deploy Storybook to GitHub Pages.
# Roda em todo push (para aparecer em Actions); publica só em main/master.
# Em Settings > Pages do repositório, defina Source = "GitHub Actions".
name: Deploy to GitHub Pages

on:
  push:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci --legacy-peer-deps
      # Corrige bug do npm com optional deps do Rollup no Linux (https://github.com/npm/cli/issues/4828)
      - run: npm install

      - run: npm run build

      - run: npm run build-storybook
        env:
          BASE_PATH: /universal-scrollbar/

      - uses: actions/upload-pages-artifact@v4
        with:
          path: storybook-static

  deploy:
    needs: build
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4
